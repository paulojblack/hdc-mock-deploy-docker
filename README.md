# hdc-mock-deploy-docker

## Setup  

Get the missing private keys from me or someone else or just spend a few hours trying to figure out where they're stashed in bamboo.  

`docker build -t [tagname] .`  
`docker run -it [tagname]`

Now you in the zone.
